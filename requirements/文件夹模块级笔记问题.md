<!-- 我好像没看到 有 文件夹级笔记 被作为特殊user 添加到Agent上下文中
先研究下逻辑

我在另一个项目中测试了 文件级笔记会正确的附加在读取的文件后面
但文件夹级笔记 并未 作为特殊user 添加到Agent上下文中(该条特殊user就没有),
```
## 📂 Folder Notebooks (Context from read files)

   The following folder notebooks are relevant to files you've read in this session. 
```
这样的模板都没出现

确保 读取一个文件后 如果该文件有文件级笔记,会正确的附加在读取的文件后面(目前正确),如果该文件没有文件级笔记,则无特殊处理.
然后该文件如果所属的文件夹有文件夹级笔记,则应该 作为特殊user 添加到Agent上下文中(目前没有),且其各层父文件夹也要检查,如果有更新的笔记,则应该更新.

正好需要调整,文件夹级笔记的读取记录 改为 和 todo 系统一样 基于文件系统保存,且支持会话间隔离,和会话恢复时,读取过的文件夹级笔记也恢复

/clear 后 新的会话中 是没有 其他会话的 文件夹级笔记的
每个会话应该相互独立的,且恢复会话后,以前读取过的文件夹级笔记应该也恢复

读文件工具,的返回是读取的文件内容 该文件如果有文件级笔记就附加上笔记返回,不是只返回笔记 -->

已完成