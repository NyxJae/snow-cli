# 需求文档: 流式 API 空闲超时重试机制

## 1. 项目现状与核心目标

### 现状

当前项目中,流式 API 响应(`createStreamingAnthropicCompletion`, `createStreamingGeminiCompletion` 等)仅在连接断开时才会触发重试机制(`withRetryGenerator`)。

**问题场景**:

- API 连接保持打开状态(未断开)
- 但长时间(如几分钟)没有收到新的数据块(chunk)
- 用户界面显示"正在生成..."但没有任何进展
- 没有机制来检测这种"假死"状态

### 核心目标

为流式 API 响应添加**空闲超时检测机制**:如果在指定时间内(默认 3 分钟)没有收到新的数据,则视为超时,触发断开重连并重试。

## 2. 范围与边界

### 功能点

- [ ] **空闲超时检测**: 在流式读取过程中,监控最后一次收到**任何数据**(包括思考内容和正式回复)的时间
- [ ] **空闲超时常量**: 超时时间固定为 3 分钟(180000ms),以常量形式存储在代码中
- [ ] **超时后重试**: 触发 `withRetryGenerator` 的重试逻辑
- [ ] **适用 API**: Anthropic、Gemini、Chat、Responses 等所有流式 API
- [ ] **断开后消息丢弃**: 任何方式断开的流式连接,若后续又收到延迟消息,必须丢弃这些消息,不得处理

### 排除项

- 不修改非流式 API 的调用
- 不改变现有的连接超时逻辑(只添加空闲超时)
- 超时时间不可配置(固定 3 分钟)
- 不影响已正常工作的流式传输

## 3. 举例覆盖需求和边缘情况

### 例 1: 正常流式传输

```
时间线:
0s      30s     60s     90s     120s    (完成)
|-------|-------|-------|-------|-------->
[chunk1][chunk2][chunk3][chunk4][DONE]

结果: 正常完成,无超时触发
```

### 例 2: 空闲超时触发重试

```
时间线:
0s      30s     60s     ...     180s(3分钟)
|-------|-------|-------|-------|-------->
[chunk1][chunk2]        (无数据)  -> 触发超时重试

结果:
1. 检测到 3 分钟无新数据
2. 抛出可重试错误
3. withRetryGenerator 捕获错误
4. 延迟后重试请求
```

### 例 3: 部分数据后空闲超时

```
时间线:
0s      30s     60s     90s     ...     270s(4.5分钟)
|-------|-------|-------|-------|-------|-------->
[chunk1][chunk2][chunk3]        (无数据 3分钟) -> 超时重试

注意: 即使已经产生过数据(hasYielded=true),空闲超时也应该触发重试
(与现有逻辑不同,现有逻辑是 hasYielded=true 时非流中断错误不重试)
```

### 例 4: 重试后成功

```
第 1 次请求:
0s      60s     180s(超时)
|-------|-------|-------->
[data]  (无响应) -> 超时断开

等待 1s (指数退避)

第 2 次请求:
0s      30s     60s
|-------|-------|-------->
[data1][data2][DONE]

结果: 重试成功,返回完整数据
```

### 边缘情况

#### 边缘 1: 恰好 3 分钟收到数据

```
0s      179s    180s
|-------|-------|-------->
[data1]         [data2]  <-- 恰好在超时前收到

结果: 不应触发超时,继续等待
```

#### 边缘 2: 用户主动中断

```
用户按 ESC / 点击停止

结果:
- 应尊重 abortSignal
- 不触发超时重试
- 立即终止
```

#### 边缘 3: 服务端长时间思考(thinking)

```
某些模型(如 Claude with thinking)可能会有长时间思考阶段

0s      60s     120s    180s    240s
|-------|-------|-------|-------|-------->
[start thinking]        [end thinking][output]
   ↑                       ↑
持续收到 thinking 数据    收到正式回复

结果:
- **思考内容(thinking content)也算作新增数据**
- 只要持续收到任何数据(包括 thinking),就不触发空闲超时
- 只有在完全没有数据返回(包括 thinking)达 3 分钟时才超时
```

#### 边缘 4: 断开后收到延迟消息(竞态条件)

```
第 1 次请求(断开场景):
0s      60s     180s(判定超时)   181s(延迟消息)   182s(新连接)
|-------|-------|--------------|--------------|------------
[chunk1][chunk2]  [断开重试]     [chunk3]       [新请求开始]
                        ↑
                  连接已标记为断开

预期行为:
1. 180s 判定超时,触发重试逻辑
2. 181s 旧连接延迟收到 chunk3
3. **chunk3 必须被丢弃,不得 yield**
4. 182s 新连接建立,从新请求获取数据

错误行为(应避免):
- chunk3 被 yield 给用户,导致数据混乱
- 新旧连接的数据混合输出
- 延迟消息被误认为是新连接的数据
```

**触发断开的场景包括但不限于:**

- 空闲超时(3 分钟无数据)
- 网络断开(connection reset/timeout)
- 用户主动中断(ESC/停止按钮)
- 服务端异常断开

**实现要点:**

- 连接断开时立即标记该连接为"已丢弃"状态
- 后续从该连接收到的任何消息都检查状态,已丢弃则忽略
- 确保标记和检查的**原子性**,防止竞态条件

## 4. 技术要点

### 实现位置

- 修改 `source/utils/core/retryUtils.ts` 中的 `withRetryGenerator`
- 或在各 API 流式读取逻辑中添加空闲检测

### 关键逻辑

#### 空闲超时检测

```typescript
// 伪代码
const STREAM_IDLE_TIMEOUT = 180000; // 3分钟,代码中固定
let lastChunkTime = Date.now();

// 启动空闲检测定时器
const idleTimer = setInterval(() => {
	if (Date.now() - lastChunkTime > idleTimeout) {
		throw new StreamIdleTimeoutError(`No data received for ${idleTimeout}ms`);
	}
}, 5000); // 每5秒检查一次

// 收到数据时更新
for await (const chunk of stream) {
	lastChunkTime = Date.now();
	yield chunk;
}

clearInterval(idleTimer);
```

#### 断开后消息丢弃机制

```typescript
// 伪代码 - 防止竞态条件
let isStreamAbandoned = false;

// 断开时标记
function onDisconnect() {
	isStreamAbandoned = true;
	reader.cancel(); // 取消读取器
}

// 读取循环中检查
for await (const chunk of stream) {
	// 关键: 检查连接是否已被丢弃
	if (isStreamAbandoned) {
		// 丢弃延迟到达的消息
		console.log('丢弃延迟消息:', chunk);
		continue;
	}
	lastChunkTime = Date.now();
	yield chunk;
}
```

**关键点:**

1. **状态标记**: 使用 `isStreamAbandoned` 布尔标记连接是否已断开
2. **检查时机**: 在每次 `yield` 前检查标记,确保延迟消息不会流出
3. **Reader 取消**: 断开时调用 `reader.cancel()` 减少延迟消息产生
4. **作用域隔离**: 每次重试创建新的 generator,确保状态不共享

### 错误类型

需要定义新的错误类型 `StreamIdleTimeoutError`,标记为可重试错误。

## 5. 常量定义

```typescript
// source/utils/core/retryUtils.ts 或相关常量文件
export const STREAM_IDLE_TIMEOUT = 180000; // 3分钟
```

## 6. 用户可见行为

### 超时重试时

```
🔄 流式响应空闲超时,正在重试 (1/5): 3分钟未收到数据,重新连接中...
```

### 最终失败时

```
❌ 流式响应失败: 已达到最大重试次数,API持续无响应
```


已完成